import { GitBarChart, GitHeatmapChart } from "@/Git";
import stats from "./stats.json";

# 2025/09/30–2025/10/03

<GitBarChart data={stats} />

## snv 少女星

### Android 打包與設定優化

- **新增 Dev 設定檔**：建立開發用設定檔，方便區分開發與正式版本。
- **優化 Gradle Build 效能**：調整參數設定，加快打包速度。
- **新增測試用資源**：新增測試用 App Icon 與 Bootsplash 畫面，改善測試版本辨識度。
- **新增 clean 指令**：增加 clean 指令，並統一其他 script 寫法以提升可維護性。
- **鎖檔更新**：刪除舊的 package-lock.json 並重產 yarn.lock，確保依賴一致性。

### Fastlane 自動化發佈流程建置

- **初始化 Fastlane 專案**：建立基本設定與結構  
  ```fastlane/init```  
  ```fastlane/Gymfile```
- **新增環境變數範例檔** (```.env.example```)，提供打包與部署設定參考  
- **整合 iOS 打包流程**  
- **測試自動發佈**：驗證 Fastlane 部署至 App Store 的流程可行性  

## 每週總結

<GitHeatmapChart data={stats} />

1. 完成 **Android 專案開發環境與打包流程優化**，提升開發體驗與建置速度。  
2. 建立清晰的 **Dev 設定檔與測試資源**，方便團隊測試與區分版本。  
3. 調整專案腳本與依賴管理，確保環境穩定、指令統一。
